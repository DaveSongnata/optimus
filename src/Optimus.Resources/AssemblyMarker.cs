namespace Optimus.Resources
{
    /// <summary>
    /// Placeholder so the SDK emits an assembly. This DLL exists only to carry the Win32
    /// icon/string resources (optimus.res) that the CorelDRAW addon framework loads for the
    /// toolbar button. It is never referenced by C# code.
    /// </summary>
    internal static class AssemblyMarker { }
}
